# Sanity Studio environment variables (gitignored — safe for local secrets).
# Only vars prefixed SANITY_STUDIO_ are exposed to the Studio (Vite convention).
# Find your project ID at https://www.sanity.io/manage or via `npx sanity projects list`.

SANITY_STUDIO_PROJECT_ID=xyz
SANITY_STUDIO_DATASET=production
